/*global describe it*/
/*global expect*/
import reducer from "__$PATH__";

describe("__$PATH__", () => {
  it("returns the initial state", () => {
    const state = void 0;
    expect(
      reducer(state, {})
    ).to.equal(null);
  });
});
