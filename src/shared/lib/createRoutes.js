/* global __DEVELOPMENT__ */
import React from "react";

import path from "path";
import routes from path.join(__dirname, "config/routes");

export default function createRoutes(store) {
    console.log(routes);
}

