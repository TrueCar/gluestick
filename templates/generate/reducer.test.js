/*global describe it*/
/*global expect*/
import reducer from "reducers/__$NAME__";

describe("reducers/__$NAME__", () => {
  it("returns the initial state", () => {
    const state = void 0;
    expect(
      reducer(state, {})
    ).to.equal(null);
  });
});
